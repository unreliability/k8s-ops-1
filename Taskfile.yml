---
version: "3"

vars:
  PROJECT_ROOT:
    sh: git rev-parse --show-toplevel

includes:
  ansible: .taskfiles/Taskfile_ansible.yaml
  deps: .taskfiles/Taskfile_{{OS}}.yaml
  # secrets: .taskfiles/Taskfile_secrets.yaml
