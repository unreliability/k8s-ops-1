---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: discord
  namespace: monitoring
spec:
  type: discord
  username: Flux
  channel: flux
  secretRef:
    name: discord-webhook-url
