all:
master:
  hosts:
    k3s-master-1:
      ansible_host: k3s-master-1.dc2.cluster
  vars:
    k3s_control_node: true
    k3s_tls_san: "k3s.dc2.cluster"

nodes:
  hosts:
    k3s-master-1:
      ansible_host: k3s-master-1
    k3s-agent-1:
      ansible_host: k3s-agent-1
    k3s-agent-2:
      ansible_host: k3s-agent-2

k3s_cluster:
  children:
    master:
    nodes:

vault:
  hosts:
    localhost:
      ansible_connection: local
